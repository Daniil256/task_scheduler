# Task Scheduler Service

Сервис для планирования и автоматического выполнения отложенных задач.  
Поддерживает перевод сотрудников, изменение статусов групп и отправку напоминаний в лог.

---

## 📌 Функциональность

- **Управление задачами через REST API:**
    - создание (`POST /tasks`);
    - просмотр всех задач (`GET /tasks`);
    - просмотр задачи по идентификатору (`GET /tasks/{id}`);
    - отмена задачи (`DELETE /tasks/{id}`).

- **Автоматическое выполнение в заданное время:**
    - перевод сотрудника в другое подразделение (изменение `group` у `Worker`);
    - изменение статуса группы (`ACTIVE` ↔ `INACTIVE`);
    - запись сообщения в лог (вывод в консоль).

- Валидация входных данных и корректные HTTP‑статусы.

---

## 🛠 Технологии

- **Java 11**
- **Spring Boot 2** (Web, Data JPA, Scheduler)
- **PostgreSQL**
- **Lombok**
- **Maven**

---

## 📡 API Endpoints

| Метод  | URL           | Описание                        | Коды ответа                         |
|--------|---------------|---------------------------------|-------------------------------------|
| POST   | `/tasks`      | Создать новую задачу            | `201 Created`, `400 Bad Request`    |
| GET    | `/tasks`      | Получить все задачи             | `200 OK`, `404 Not Found`           |
| GET    | `/tasks/{id}` | Получить задачу по ID           | `200 OK`, `404 Not Found`           |
| DELETE | `/tasks/{id}` | Отменить запланированную задачу | `204 NO CONTENT`, `400 Bad Request` |

---

## 📦 Модели данных

### Запросы (`TaskRequest`)

Полиморфный JSON, тип задачи определяется полем `taskType`.

#### 1. Перевод сотрудника (`TRANSFER`)

```json
{
  "taskType": "TRANSFER",
  "scheduledTime": "2026-03-15T10:00:00",
  "workerId": 1,
  "targetGroupId": 2
}
```

#### 2. Изменение статуса группы (`GROUP_STATUS`)

```json
{
  "taskType": "GROUP_STATUS",
  "scheduledTime": "2026-03-20T00:00:00",
  "groupId": 3,
  "groupStatus": "INACTIVE"
}
```

#### 3. Запись в лог (`LOG`)

```json
{
  "taskType": "LOG",
  "scheduledTime": "2026-03-25T12:00:00",
  "message": "Напоминание о собрании"
}
```

### Ответы (`TaskResponse`)

```json
{
  "id": 1,
  "taskType": "TRANSFER",
  "scheduledTime": "2026-03-15T10:00:00",
  "status": "SCHEDULED"
}
```

---

## 🚀 Запуск проекта

1. Клонируйте репозиторий:
   ```bash
   git clone
    ```
2. Запустите [docker-compose.yml](docker-compose.yml) для установки контейнера postgres.
3. Добавите сущности в базу данных

```sql
INSERT INTO groups (group_name, group_status)
VALUES ('Разработка', 'ACTIVE'),
       ('Тестирование', 'ACTIVE'),
       ('Аналитика', 'ACTIVE'),
       ('DevOps', 'ACTIVE'),
       ('Техподдержка', 'ACTIVE'),
       ('HR', 'INACTIVE'),
       ('Бухгалтерия', 'INACTIVE'),
       ('Маркетинг', 'ACTIVE'),
       ('Продажи', 'ACTIVE'),
       ('Безопасность', 'INACTIVE');

INSERT INTO workers (worker_name, status, group_id)
VALUES ('Иван Петров', 'ACTIVE', 1),
       ('Мария Смирнова', 'ACTIVE', 1),
       ('Алексей Козлов', 'ACTIVE', 1),
       ('Елена Новикова', 'ACTIVE', 1),
       ('Дмитрий Волков', 'INACTIVE', 1),
       ('Петр Сидоров', 'ACTIVE', 2),
       ('Анна Морозова', 'ACTIVE', 2),
       ('Сергей Иванов', 'ACTIVE', 2),
       ('Ольга Соколова', 'INACTIVE', 2),
       ('Михаил Федоров', 'ACTIVE', 3),
       ('Татьяна Павлова', 'ACTIVE', 3),
       ('Андрей Степанов', 'ACTIVE', 3),
       ('Константин Орлов', 'ACTIVE', 4),
       ('Юлия Васильева', 'ACTIVE', 4),
       ('Григорий Михайлов', 'ACTIVE', 4),
       ('Наталья Белова', 'ACTIVE', 5),
       ('Павел Семенов', 'ACTIVE', 5),
       ('Екатерина Зайцева', 'ACTIVE', 5),
       ('Артем Виноградов', 'ACTIVE', 5),
       ('Виктория Крылова', 'ACTIVE', 6),
       ('Денис Лебедев', 'ACTIVE', 6),
       ('Людмила Соколова', 'ACTIVE', 7),
       ('Борис Новиков', 'INACTIVE', 7),
       ('Алина Громова', 'ACTIVE', 8),
       ('Максим Тарасов', 'ACTIVE', 8),
       ('Светлана Ким', 'ACTIVE', 8),
       ('Владислав Мороз', 'ACTIVE', 9),
       ('Ксения Соболева', 'ACTIVE', 9),
       ('Роман Жуков', 'ACTIVE', 9),
       ('Анастасия Волкова', 'ACTIVE', 9),
       ('Игорь Смирнов', 'ACTIVE', 10),
       ('Валентина Петрова', 'INACTIVE', 10)
```

4. Запустите приложение:
5. ```bash
   mvn spring-boot:run
   ```
6. Используйте Postman или готовый [test_api.http](test_api.http) для взаимодействия с API.
